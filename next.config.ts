import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["placehold.co", "picsum.photos", "scontent.cdninstagram.com"],
  },
  reactStrictMode: true,
};

export default nextConfig;
